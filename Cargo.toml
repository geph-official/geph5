[workspace]

members = ["libraries/*", "binaries/*"]
resolver = "2"

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"
opt-level = 1


[profile.release-dbg]
inherits = "dev"
opt-level = 3
